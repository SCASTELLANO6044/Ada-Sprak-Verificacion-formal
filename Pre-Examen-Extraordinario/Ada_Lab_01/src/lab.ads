--  Este paquete esta vacio. Utilizalo para anadir los ejemplos de
--  la siguiente pagina que quieras utilizar para familiarizarte con
--  Ada:
--    https://learn.adacore.com/courses/intro-to-ada/index.html

--  Recuerda que:
--    1) En este fichero (fichero lab.ads) debes poner la especificacion
--       de los procedimientos o funciones que quieras probar.
--
--          La especificacion es la cabecera del procedimiento (desde
--          la palabra 'procedure' o 'function') y sus parametros. En
--          el caso de las funciones hay que anadir tambien el tipo
--          de dato que devuelven.
--
--    2) En el cuerpo del paquete (fichero lab.adb) debes copiar el codigo
--       del cuerpo del procedimiento que quieras probar.
--
--    3) En el fichero main.adb debes anadir la llamada al procedimiento
--       o funcion que quieras probar.

package Lab is


end Lab;
